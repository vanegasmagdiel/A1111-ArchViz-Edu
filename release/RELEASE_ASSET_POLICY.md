# Release Asset Policy

The release ZIP is uploaded as a GitHub Release asset, not committed to the Git history. Large third-party model weights are never uploaded by this repository. The installer downloads them from the upstream model repositories recorded in the frozen catalog.
